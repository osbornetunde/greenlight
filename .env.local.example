# Local Environment Overrides Example
# Copy this file to .env.local to override specific settings for your local development
# Usage: cp .env.local.example .env.local

# NOTE: .env.local is git-ignored and will override values from .env

# =============================================================================
# LOCAL DEVELOPMENT OVERRIDES
# =============================================================================

# Override server port for local testing
# PORT=5000

# Override environment for testing
# ENV=local

# Override database for local testing
# GREENLIGHT_DB_DSN=postgres://dev:dev@localhost/greenlight_test?sslmode=disable

# Override SMTP for personal testing
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-personal-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Override CORS for different frontend ports
# CORS_TRUSTED_ORIGINS="http://localhost:3001 http://localhost:8080"

# Disable rate limiting for easier development
# LIMITER_ENABLED=false

# =============================================================================
# COMMON LOCAL OVERRIDE SCENARIOS
# =============================================================================

# Scenario 1: Testing with different database
# GREENLIGHT_DB_DSN=postgres://user:pass@localhost/greenlight_experimental

# Scenario 2: Testing with personal email provider
# SMTP_HOST=smtp.gmail.com
# SMTP_USERNAME=yourname@gmail.com
# SMTP_PASSWORD=your-16-char-app-password

# Scenario 3: Frontend development on different port
# PORT=3001
# CORS_TRUSTED_ORIGINS="http://localhost:3000"

# Scenario 4: Performance testing without rate limiting
# LIMITER_ENABLED=false
# PORT=8000

# =============================================================================
# USAGE NOTES
# =============================================================================

# 1. Copy this file: cp .env.local.example .env.local
# 2. Uncomment and modify the values you want to override
# 3. The .env.local file will be automatically loaded by .envrc (if using direnv)
# 4. .env.local takes precedence over .env values
# 5. Command-line flags still take highest precedence

# Load order (lowest to highest priority):
# 1. .env file (shared defaults)
# 2. .env.local file (your personal overrides)
# 3. Environment variables (export VAR=value)
# 4. Command-line flags (-flag=value)
